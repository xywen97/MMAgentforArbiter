# MMAgent: Mathematical Modeling Agent

MMAgent 是一个基于大语言模型（LLM）的自动化数学建模问题求解系统。该系统通过多阶段协作的方式，将复杂的数学建模问题分解为可管理的子任务，自动进行问题分析、数学建模、代码生成与执行，最终生成完整的解决方案。

## 📋 目录

- [系统概述](#系统概述)
- [核心特性](#核心特性)
- [系统架构](#系统架构)
- [安装与配置](#安装与配置)
- [使用方法](#使用方法)
- [工作流程](#工作流程)
- [目录结构](#目录结构)
- [配置说明](#配置说明)
- [输出说明](#输出说明)
- [支持的模型](#支持的模型)

## 🎯 系统概述

MMAgent 采用分阶段、多 Agent 协作的架构，通过以下四个主要阶段解决数学建模问题：

1. **问题分析阶段**：理解问题背景、需求，进行高层次建模
2. **任务分解阶段**：将复杂问题分解为多个子任务
3. **数学建模阶段**：为每个子任务进行数学建模和方法检索
4. **计算求解阶段**：生成代码、执行计算、解释结果

## ✨ 核心特性

### 1. Actor-Critic-Improvement 循环机制
- 采用多轮改进机制提升问题分析和建模质量
- 通过批判和改进循环不断优化解决方案

### 2. 智能任务依赖管理
- 自动构建任务依赖图（DAG）
- 通过拓扑排序确定最优执行顺序
- 支持任务间信息传递和代码复用

### 3. 层次化方法检索（RAG）
- 从 HMML 知识库中检索相关建模方法
- 使用层次化评分机制（父节点 + 子节点权重）
- 支持基于嵌入向量和 LLM 的混合检索

### 4. 自动化代码生成与执行
- 自动生成 Python 代码
- 支持代码执行和错误调试
- 多轮重试机制确保代码正确性
- 自动提取代码结构供后续任务使用

### 5. 记忆机制
- 任务结果记忆：存储每个任务的完整信息
- 代码结构记忆：存储代码输出文件信息
- 支持任务间信息传递和依赖关系处理

## 🏗️ 系统架构

### 核心 Agent 组件

| Agent | 功能 | 位置 |
|-------|------|------|
| **ProblemUnderstanding** | 问题理解与分析，高层次建模 | `agent/problem_analysis.py` |
| **ProblemDecompose** | 问题分解为子任务 | `agent/problem_decompse.py` |
| **Coordinator** | 任务依赖分析与 DAG 构建 | `agent/coordinator.py` |
| **MethodRetriever** | 方法检索（RAG） | `agent/retrieve_method.py` |
| **TaskSolver** | 任务求解（分析、建模、编码、结果解释） | `agent/task_solving.py` |
| **DataDescription** | 数据描述总结 | `agent/data_description.py` |
| **ChartCreator** | 图表生成 | `agent/create_charts.py` |

### 工具模块

| 模块 | 功能 | 位置 |
|------|------|------|
| **problem_analysis** | Stage 1 流程编排 | `utils/problem_analysis.py` |
| **mathematical_modeling** | Stage 2 流程编排 | `utils/mathematical_modeling.py` |
| **computational_solving** | Stage 3 流程编排 | `utils/computational_solving.py` |
| **utils** | 文件读写、格式转换、目录创建 | `utils/utils.py` |
| **embedding** | 向量检索 | `utils/embedding.py` |

## 📦 安装与配置

### 环境要求

- Python 3.12
- GPU * 1, for local embedding model: Alibaba-NLP/gte-multilingual-base, to calculate the similarity

### 安装依赖

```bash
pip install -r requirements.txt
```

### 配置文件

创建 `config.yaml` 文件：

```yaml
problem_analysis_round: 3        # 问题分析改进轮数
problem_modeling_round: 3         # 问题建模改进轮数
tasknum: 4                        # 任务分解数量
top_method_num: 6                # 检索方法数量
task_formulas_round: 1           # 公式生成改进轮数
chart_num: 2                     # 每个任务生成的图表数量
```

## 🚀 使用方法

### 基本用法: bash run.sh

```bash
python MMAgent/main.py \
    --key sk-... \
    --base_url https://api.example.com/v1 \
    --task 2024_C \
    --model_name gpt-4o-mini \
    --method_name MM-Agent
```

### 命令行参数

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `--key` | str | `''` | API 密钥 |
| `--task` | str | `2024_C` | 任务名称（对应 `MMBench/problem/` 下的问题文件） |
| `--model_name` | str | `gpt-4o` | LLM 模型名称 |
| `--method_name` | str | `MM-Agent` | 方法名称（用于输出目录命名） |
| `--base_url` | str | `None` | 自定义 API 端点 URL（可选） |


## 🔄 工作流程

### 阶段 1: 问题分析

```
问题 JSON 文件
    ↓
数据描述总结 (DataDescription)
    ↓
问题理解 (ProblemUnderstanding)
    ├─> Actor: 初始分析
    ├─> Critic: 批判分析
    └─> Improvement: 改进（循环 N 轮）
    ↓
高层次建模 (ProblemUnderstanding.modeling)
    ├─> Actor: 初始建模
    ├─> Critic: 批判建模
    └─> Improvement: 改进（循环 N 轮）
    ↓
任务分解 (ProblemDecompose)
    ├─> decompose(): 分解为子任务
    └─> refine(): 细化每个任务描述
    ↓
依赖分析 (Coordinator)
    ├─> analyze(): 分析任务依赖关系
    ├─> dag_construction(): 构建 DAG
    └─> compute_dag_order(): 拓扑排序
```

**输出**：
- `problem`: 问题信息字典
- `order`: 任务执行顺序（拓扑排序）
- `with_code`: 是否需要代码执行
- `coordinator`: 协调器对象（含 DAG 和 memory）
- `task_descriptions`: 任务描述列表
- `solution`: 初始解决方案字典

### 阶段 2: 数学建模（对每个任务）

```
任务依赖提示词构建
    ↓
任务分析 (TaskSolver.analysis)
    ↓
方法检索 (MethodRetriever)
    ├─> 层次化评分 (MethodScorer)
    │   ├─> embedding_scorer 或 llm_score_method
    │   └─> 计算最终分数
    └─> 返回 top_k 个最相关方法
    ↓
数学建模 (TaskSolver.modeling)
    ├─> formulas_actor: 生成数学公式
    ├─> formulas_critic: 批判公式
    ├─> formulas_improvement: 改进公式（循环 N 轮）
    └─> modeling_actor: 生成建模过程
```

**输出**：
- `task_description`: 任务描述
- `task_analysis`: 任务分析
- `task_modeling_formulas`: 数学公式
- `task_modeling_method`: 建模方法
- `dependent_file_prompt`: 依赖文件提示词

### 阶段 3: 计算求解（对每个任务）

```
代码生成与执行 (TaskSolver.coding)
    ├─> coding_actor: 生成代码
    │   ├─> LLM 生成代码
    │   ├─> 保存代码文件
    │   └─> execute_script: 执行代码
    └─> coding_debugger: 调试代码（如果失败）
        ├─> 根据错误修复代码
        └─> 重新执行（最多重试 5 次）
    ↓
代码结构提取 (TaskSolver.extract_code_structure)
    └─> 解析代码输出文件信息
    ↓
结果解释 (TaskSolver.result)
    └─> 根据执行结果解释任务结果
    ↓
答案生成 (TaskSolver.answer)
    └─> 生成最终答案
    ↓
图表生成 (ChartCreator.create_charts)
    └─> 循环生成 N 个图表
    ↓
保存到 memory 和 solution
```

**输出**：
- 更新 `solution` 字典
- 保存代码文件到 `output_dir/code/`
- 保存解决方案到 `output_dir/json/` 和 `output_dir/markdown/`

### 阶段 4: 解决方案报告（可选）

```python
# 当前已注释，可通过取消注释启用
generate_paper(llm, output_dir, name)
```

## 📁 目录结构

```
run.sh
config.yaml
README.md
MMBench/
├── dataset/                
├── evaluation/
├── example_solution/
├── problem/
├── __init__.py
└── README.md
MMAgent/
├── main.py                 # 主入口文件
├── config.yaml             # 配置文件
├── .env                    # 环境变量配置（需自行创建）
├── agent/                  # Agent 组件
│   ├── base_agent.py      # Agent 基类
│   ├── problem_analysis.py
│   ├── problem_decompse.py
│   ├── coordinator.py
│   ├── retrieve_method.py
│   ├── task_solving.py
│   ├── data_description.py
│   └── create_charts.py
├── llm/                    # LLM 接口
│   └── llm.py
├── utils/                  # 工具模块
│   ├── problem_analysis.py
│   ├── mathematical_modeling.py
│   ├── computational_solving.py
│   ├── utils.py
│   ├── embedding.py
│   └── ...
├── prompt/                 # 提示词模板
│   ├── template.py
│   ├── decompose_prompt.json
│   └── constants.py
├── HMML/                   # 数学建模方法知识库
│   ├── HMML.json
│   └── HMML.md
├── code_template/          # 代码模板
│   ├── main1.py
│   ├── main2.py
│   └── ...
└── output/                 # 输出目录
    └── {method_name}/
        └── {task}_{timestamp}/
            ├── json/       # JSON 格式解决方案
            ├── markdown/   # Markdown 格式解决方案
            ├── code/       # 生成的代码文件
            └── usage/      # API 使用统计
```

## ⚙️ 配置说明

### config.yaml 参数说明

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `problem_analysis_round` | 问题分析改进轮数，越多质量越高但耗时越长 | 3 |
| `problem_modeling_round` | 问题建模改进轮数 | 3 |
| `tasknum` | 任务分解数量，根据问题复杂度调整 | 4 |
| `top_method_num` | 从知识库检索的方法数量 | 6 |
| `task_formulas_round` | 公式生成改进轮数 | 1 |
| `chart_num` | 每个任务生成的图表数量 | 2 |

### 问题文件格式

问题文件应放在 `MMBench/problem/` 目录下，格式为 JSON：

```json
{
  "background": "问题背景描述",
  "problem_requirement": "问题需求描述",
  "dataset_path": "数据集路径（可选）",
  "dataset_description": "数据集描述（可选）",
  "variable_description": "变量描述（可选）",
  "addendum": "补充说明（可选）"
}
```

## 📊 输出说明

### 输出目录结构

```
output/{method_name}/{task}_{timestamp}/
├── json/
│   └── {task}.json          # 完整解决方案（JSON 格式）
├── markdown/
│   └── {task}.md            # 完整解决方案（Markdown 格式）
├── code/
│   ├── main1.py             # 任务 1 的代码
│   ├── main2.py             # 任务 2 的代码
│   └── ...                  # 其他任务代码和数据文件
└── usage/
    ├── {task}.json          # API 使用统计（JSON）
    └── runtime.txt          # 运行时间统计
```

### 解决方案 JSON 结构

```json
{
  "problem_background": "问题背景",
  "problem_requirement": "问题需求",
  "problem_analysis": "问题分析",
  "tasks": [
    {
      "task_description": "任务描述",
      "task_analysis": "任务分析",
      "preliminary_formulas": "数学公式",
      "mathematical_modeling_process": "建模过程",
      "task_code": "代码（如果有）",
      "is_pass": true,
      "execution_result": "执行结果",
      "solution_interpretation": "结果解释",
      "subtask_outcome_analysis": "答案分析",
      "charts": ["图表描述1", "图表描述2"]
    }
  ]
}
```

## 🤖 支持的模型

### OpenAI 兼容模型
- `gpt-4o`
- `gpt-4`
- `...`

### DeepSeek 模型
- `deepseek-chat`
- `deepseek-reasoner`

### 阿里云模型
- `qwen2.5-72b-instruct`

### 自定义模型
通过 `--base_url` 参数支持任何 OpenAI 兼容的 API 端点。

## 🔍 关键设计特点

### 1. Actor-Critic-Improvement 循环
- 通过多轮改进机制提升问题分析和建模质量
- 每次循环都会对前一轮结果进行批判和改进

### 2. 任务依赖管理
- 自动构建任务依赖图（DAG）
- 通过拓扑排序确定最优执行顺序
- 后续任务可以访问依赖任务的结果和代码结构

### 3. 层次化方法检索
- 从 HMML 知识库中检索相关建模方法
- 使用层次化评分机制（父节点权重 + 子节点权重）
- 支持基于嵌入向量和 LLM 的混合检索

### 4. 代码生成与执行
- 自动生成、执行、调试代码
- 支持多轮重试和迭代调试
- 自动提取代码结构供后续任务使用

### 5. 记忆机制
- `coordinator.memory`: 存储每个任务的完整信息
- `coordinator.code_memory`: 存储代码结构信息
- 支持任务间信息传递和依赖关系处理